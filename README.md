# vps_apps

These are simple apps that I am running on a VPS.
One is just a simple static landing page, the other is a
dynamic site that uses MongoDB and PostgreSQL.

Both apps run on express. The landing page is just a
simple server that sends an HTML file on request. The
dynamic site is a simple forum app currently only using
PostgreSQL but will be updated soon with any luck.
